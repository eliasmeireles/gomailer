# Resend API client against the local mock (mockapi service). No account needed: use the console
# "Simulate failures" panel to make the mock fail and exercise retries and error codes.
MAILER_TRANSPORT=api
MAILER_API_CLIENT=resend
RESEND_API_KEY=re_dev_mock
RESEND_API_URL=http://mockapi:9100
