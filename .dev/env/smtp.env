# Local SMTP (Mailpit, implicit TLS). Works out of the box: open http://localhost:8025 to see the emails.
MAILER_TRANSPORT=smtp
SMTP_SERVER=mailpit
SMTP_SERVER_PORT=1025
SMTP_SERVER_USER=mailer
SMTP_SERVER_PASS=mailer
